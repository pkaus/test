---
- hosts: localhost
  connection: localhost
  become: true

  tasks:
  - name: install packages
    package:
      name:
        - htop
        - tmux

- name: copy wallpaper file
  copy:
    src: files/file.png
    dest: /usr/share/
    owner: root
    group: root

- name: set wallpaper
